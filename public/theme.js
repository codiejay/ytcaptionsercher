import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    Heading: `Clashbold', sans-serif`,
    body: `Clashmid', sans-serif`,
    span: `Clashreg', sans-serif`,
  },
});

export default theme;
